name: Labeler (migrated to Azure Pipelines)

on:
  workflow_dispatch:

jobs:
  migrated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic GitHub PR labeling has been disabled while CI/CD moves to Azure Pipelines."
