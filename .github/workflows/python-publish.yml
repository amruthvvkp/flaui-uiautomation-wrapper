name: Publish Python distributions (migrated to Azure Pipelines)

on:
  workflow_dispatch:

jobs:
  migrated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Package build and PyPI deployment moved to azure-pipelines.yml."
